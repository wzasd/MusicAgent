[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    integration: requires ChromaDB + LLM API
    e2e: requires running server
    regression: intent classification regression tests
    webhook: webhook handler regression tests
    evaluation: RAG retrieval quality evaluation
    slow: slow tests (e.g., web search)
